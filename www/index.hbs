{{page "description" "Welcome to the portfolio of Eric Cornelissen!"}}
{{page "keywords" "portfolio,contributions"}}

<!DOCTYPE html>
<html lang="{{site.lang}}">
  <head>
    {{> includes/metadata}}
  </head>
  <body>
    {{> includes/header no_breadcrumbs=true}}

    <main class="main">
      <section id="history" class="section section--full-width">
        <div class="timeline timeline--horizontal">
          <a class="timeline-event timeline-event--more" aria-label="View full timeline" href="/timeline" rel="noopener"></a>
          {{#each (reverse (first timeline 4))}}
            <div class="timeline-event timeline-event--{{ type }}">
              <p class="timeline-event__title short-underline">{{ title }}</p>
              <p><span class="text-italic">{{ when }}</span> | {{ institute }}</p>
              <a class="timeline-event__more more-arrow" href="{{ link }}" rel="noopener">More</a>
            </div>
          {{/each}}
        </div>
      </section>

      <section id="projects" class="section">
        <h2 class="short-underline">Projects</h2>
        <p>A selection of the favorite projects I worked on. You can find a list of all my projects <a class="text-link" href="/archive#projects" rel="noopener">over here</a>.</p>

        <ul class="list list--cards">
          {{#each projects}}
            {{#if spotlight}}
              <li class="list-item">
                <div class="list-item__header">
                  <h3 class="list-item__title">{{name}}</h3>

                  <ul class="list-item__links">
                    {{#each links as |url key|}}
                      <li class="list-item__link">
                        <a class="icon icon-{{@key}} icon--hover" aria-label="link to the project's {{get_name key}}" href="{{url}}" rel="noopener" target="_blank"></a>
                      </li>
                    {{/each}}
                  </ul>
                </div>

                <p class="list-item__description">{{{truncate (sanitize tldr) 80}}}</p>
                <a class="list-item__more more-arrow" href="/archive/{{hyphenate (lowercase name)}}" rel="noopener">More</a>
              </li>
            {{/if}}
          {{/each}}
        </ul>
      </section>

      <section id="contributions" class="section">
        <h2 class="short-underline">Contributions</h2>
        <p>As I said, I love contributing to open source software. Here are a few of the favorite projects I contributed to. You can find a complete overview <a class="text-link" href="/archive#contributions" rel="noopener">here</a>.</p>

        <ul class="list list--cards">
          {{#each contributions}}
            {{#if spotlight}}
              <li class="list-item">
                <div class="list-item__header">
                  <h3 class="list-item__title">{{name}}</h3>

                  <ul class="list-item__links">
                    {{#each links as |url key|}}
                      <li class="list-item__link">
                        <a class="icon icon-{{@key}} icon--hover" aria-label="link to the project's {{get_name key}}" href="{{url}}" rel="noopener" target="_blank"></a>
                      </li>
                    {{/each}}
                  </ul>
                </div>

                <p class="list-item__description">{{{ellipsis (sanitize about) 80}}}</p>
                <a class="list-item__more more-arrow" href="/archive/{{hyphenate (lowercase name)}}" rel="noopener">More</a>
              </li>
            {{/if}}
          {{/each}}
        </ul>
      </section>

      {{> includes/back-to-top}}
    </main>

    {{> includes/footer}}
  </body>
</html>
