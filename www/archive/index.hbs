{{page "title" "Archive"}}
{{page "description" "An overview of the projects I've worked on, and projects I've contributed to"}}
{{page "keywords" "portfolio,archive,contributions"}}

<!DOCTYPE html>
<html lang="{{site.lang}}">
  <head>
    {{> includes/metadata}}
    <link href="/styles/bundle.css" media="screen" rel="stylesheet" type="text/css">
  </head>
  <body>
    {{> includes/header compact=true}}

    <main class="main">
      <section id="projects" class="section">
        <h2 class="short-underline">Programming &amp; Web development</h2>
        <div class="ordered">
          {{#each projects as |project_list year|}}
            <div class="ordered-item" data-order="{{year}}">
              <h3 class="category">{{year}}</h3>
              <ul class="cardboard">
                {{#each project_list}}
                  <li class="card">
                    <div class="card__header">
                      <h3 class="card__title">{{name}}</h3>

                      <ul class="list-of-icons">
                        {{#each links as |url key|}}
                          <li class="list-item">
                            <a class="icon icon-{{@key}} icon--hover" aria-label="link to the project's {{get_name key}}" href="{{url}}" rel="noopener" target="_blank"></a>
                          </li>
                        {{/each}}
                      </ul>
                    </div>

                    <p class="card__description">{{{truncate (sanitize tldr) 80}}}</p>
                    <a class="card__more more-arrow" href="/archive/{{hyphenate (lowercase name)}}" rel="noopener">More</a>
                  </li>
                {{/each}}
              </ul>
            </div>
          {{/each}}
        </div>
      </section>

      <section id="contributions" class="section">
        <h2 class="short-underline">Contributions</h2>
        <div class="ordered">
          {{#each contributions as |project_list year|}}
            <div class="ordered-item" data-order="{{year}}">
              <h3 class="category">{{year}}</h3>
              <ul class="cardboard">
                {{#each project_list}}
                  <li class="card">
                    <div class="card__header">
                      <h3 class="card__title">{{name}}</h3>

                      <ul class="list-of-icons">
                        {{#each links as |url key|}}
                          <li class="list-item">
                            <a class="icon icon-{{@key}} icon--hover" aria-label="link to the project's {{get_name key}}" href="{{url}}" rel="noopener" target="_blank"></a>
                          </li>
                        {{/each}}
                      </ul>
                    </div>

                    <p class="card__description">{{{ellipsis (sanitize about) 80}}}</p>
                    <a class="card__more more-arrow" href="/archive/{{hyphenate (lowercase name)}}" rel="noopener">More</a>
                  </li>
                {{/each}}
              </ul>
            </div>
          {{/each}}
        </div>
      </section>

      {{> includes/back-to-top}}
    </main>

    {{> includes/footer}}
  </body>
</html>
